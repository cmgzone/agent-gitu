{
  "name": "hermes-agent",
  "version": "0.1.0",
  "description": "Hermes: a bounded autonomous engineering agent with project guard, task ledger, evidence gates, and loop prevention",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "hermes": "dist/cli.js"
  },
  "scripts": {
    "start": "node dist/cli.js",
    "ui": "node dist/cli.js ui",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "hermes": "tsx src/cli.ts",
    "app": "npm run build && electron desktop/main.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^43.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
